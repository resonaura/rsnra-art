import original from 'react95/dist/themes/original';

// Keep the authentic Windows 95 palette — this is the single source of
// truth for theming. Override individual tokens here if ever needed.
export const theme = original;
